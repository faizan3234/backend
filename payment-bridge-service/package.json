{
  "name": "reliv-payment-bridge",
  "version": "1.0.0",
  "description": "Trusted payment verification bridge for offline Reliv kiosk",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check paymentV2Crypto.js && node --check paymentV2Service.js && node --check paymentV2Routes.js && node --check paymentV2Db.js && node --check generate-payment-v2-cloud-key.js",
    "test": "node test-payment-v2-cloud.js",
    "dev": "node --watch index.js",
    "generate-keys": "node generate-keys.js",
    "generate-v2-keys": "node generate-payment-v2-cloud-key.js",
    "init-db": "node init-db.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "razorpay": "^2.9.2",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
